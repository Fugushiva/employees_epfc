{% extends 'base.html.twig' %}

{% block title %}Leave{% endblock %}

{% block body %}
    <h1>Leave</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ leave.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ leave.type }}</td>
            </tr>
            <tr>
                <th>Begin</th>
                <td>{{ leave.begin ? leave.begin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ leave.end ? leave.end|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_leave_index') }}">back to list</a>

    <a href="{{ path('app_leave_edit', {'id': leave.id}) }}">edit</a>

    {{ include('leave/_delete_form.html.twig') }}
{% endblock %}
