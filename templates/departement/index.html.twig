{% extends 'base.html.twig' %}

{% block title %}Hello DepartementController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <!--Jérôme-->
    <h1>Départements</h1>

    {% for dep in departements %}
        <ul><h3>{{dep.deptname}}</h3>
        <li>{{dep.description}}</li>
        {% set img = dep.getManager is not null ? dep.getManager.getEmployee.getPhoto~'.png':'' %}
       <img src='' >
        <!--Iterations sur les employés-->
        {% set count = 0 %}
        {% for employee in dep.getEmployees %}
                {% set count = count +1 %}
        {% endfor %}
        <li>{{count}} employés dans ce secteur</li>
         


        </ul>
    {% else %}
        <p>rien a afficher</p>
    {% endfor %}

        

 <p><a href="{{path('app_departement_new')}}">ajouter</a></p>
 
</div>
{% endblock %}
